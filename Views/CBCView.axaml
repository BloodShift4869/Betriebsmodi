<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:Betriebsmodi.ViewModels"
             mc:Ignorable="d"
             x:Class="Betriebsmodi.Views.CBCView"
             x:DataType="vm:CBCViewModel">
    <Grid RowDefinitions="40,40,40,40,40,40,40,40,40,40" RowSpacing="10" ColumnDefinitions="Auto, Auto">
        <!-- User input (character n) -->
        <Border Grid.Column="0" Grid.Row="0" Width="40" Height="40" Margin="10 0 0 0" Background="LightGray"
                HorizontalAlignment="Left">
            <TextBlock x:Name="CharacterN" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"
                       Text="{Binding CharacterN }" />
        </Border>
        <!-- Bit representation -->
        <ItemsControl ItemsSource="{Binding CharacterString}" Grid.Row="1" Grid.Column="0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="40" Height="40" Margin="10 0 0 0" Background="LightGray">
                        <TextBlock Text="{Binding}" FontSize="24"
                                   VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <!-- IV -->
        <ItemsControl ItemsSource="{Binding NonceString}" Grid.Row="2" Grid.Column="0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="40" Height="40" Margin="10 0 0 0" Background="LightGray">
                        <TextBlock Text="{Binding}" FontSize="24"
                                   VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <!-- XOR icon -->
        <Image Grid.Column="0" Grid.Row="3" Width="40" Height="40" HorizontalAlignment="Center"
               Source="/Assets/xor.png" />
        <!-- Interim result -->
        <ItemsControl ItemsSource="{Binding InterimString}" Grid.Row="4" Grid.Column="0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="40" Height="40" Margin="10 0 0 0" Background="LightGray">
                        <TextBlock Text="{Binding}" FontSize="24"
                                   VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <!-- Key -->
        <ItemsControl ItemsSource="{Binding KeyString}" Grid.Row="5" Grid.Column="0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="40" Height="40" Margin="10 0 0 0" Background="LightGray">
                        <TextBlock Text="{Binding}" FontSize="24"
                                   VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <!-- XOR icon -->
        <Image Grid.Column="0" Grid.Row="6" Width="40" Height="40" HorizontalAlignment="Center"
               Source="/Assets/xor.png" />
        <!-- Cipher -->
        <ItemsControl ItemsSource="{Binding CipherString}" Grid.Row="7" Grid.Column="0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="40" Height="40" Margin="10 0 0 0" Background="LightGray">
                        <TextBlock Text="{Binding}" FontSize="24"
                                   VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <!-- P-box icon -->
        <Image Grid.Column="0" Grid.Row="8" Height="40" Width="250" Stretch="Fill"
               Source="/Assets/pbox.png" />
        <!-- Output block n -->
        <ItemsControl ItemsSource="{Binding OutputString}" Grid.Row="9" Grid.Column="0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="40" Height="40" Margin="10 0 0 0" Background="LightGray">
                        <TextBlock Text="{Binding}" FontSize="24"
                                   VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Border Grid.Row="0" Grid.Column="1" Grid.RowSpan="7" Margin="10 0" Background="Black" Width="2.5" />
    </Grid>
</UserControl>